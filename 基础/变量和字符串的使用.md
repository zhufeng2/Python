# 变量和字符串的使用
## 变量
**变量的命名需要注意的事项**
1. 变量名只能包含字母，数字和下划线
2. 不能包含空格
3. 不能将python的关键字或者函数名作变量名

## 字符串
用单引号或者双引号包含在其中
字符串常用的操作：
![](https://raw.githubusercontent.com/zhufeng2/image/master/%7B2EB2BE9A-6024-4DE0-A3AB-610D40EC1792%7D.xps.png)

需要注意的一点：用+时，不能把int型和str型的数据加到一起，否则会报错，例如23，因为python无法理解到底是字符2和3还是23，我们可以定义为str型这样就可以使用+和其他的str型数据连接到一起了，`str(23)`

